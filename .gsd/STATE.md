# State Memory

**Current Phase:** Phase 2 (Design Pivot & GSD Overhaul)
**Current Wave:** Wave 1 (Memory & Design System)

## Context Notes
- We are using Tailwind v4, so the design system is implemented via `@theme` in `globals.css` rather than a `tailwind.config.ts`.
- The `ScrollSequence` component has been modified to serve as the Hero section and flawlessly picks up from the first frame.

## Next Steps
- Execute Wave 2 (Component Atomicity) by breaking down `page.tsx`.
